import React, { FunctionComponent } from 'react';
import GlobalStyle from 'styles';

const Root: FunctionComponent = () => (
  <React.StrictMode>
    <GlobalStyle />
  </React.StrictMode>
);

export default Root;
